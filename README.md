# Ray: Write once, run wherever you want.

Ray is a dynamic programming language that focuses on these main features:
- A simple VM (RayVM) that you could implement almost anywhere - even in Scratch mods
- Readability of code
- Ease of packaging (code compiles down to a single .rvm bytecode file)
Because of the various runtime environments, a compatibility syntax (ex. @require: gui, gpu) is also going to be developed.

# Roadmap

- [ ] Finally change the name of the project to Ray instead of Lolscript
- [ ] Dictionaries
- [ ] More advanced imports
- [ ] Classes (via prototypes)
- [ ] Async support (through a @require flag)
- [ ] A standard library (though certain imports, like fs, would need an @require flag)
- [ ] For loops
- [x] While loops
